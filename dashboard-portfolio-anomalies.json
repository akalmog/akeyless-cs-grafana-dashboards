{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "type": "stat",
      "title": "SLA",
      "gridPos": {
        "h": 4,
        "w": 3,
        "x": 0,
        "y": 0
      },
      "id": 1,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Account ID(s)",
      "gridPos": {
        "h": 4,
        "w": 4,
        "x": 3,
        "y": 0
      },
      "id": 2,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT account_id AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT account_id AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Company Name",
      "gridPos": {
        "h": 4,
        "w": 5,
        "x": 7,
        "y": 0
      },
      "id": 3,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT name AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT name AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Contract Start Date",
      "gridPos": {
        "h": 4,
        "w": 3,
        "x": 12,
        "y": 0
      },
      "id": 4,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(current_contract_start_date, 'N/A') AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT COALESCE(current_contract_start_date, 'N/A') AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Contract End Date",
      "gridPos": {
        "h": 4,
        "w": 3,
        "x": 15,
        "y": 0
      },
      "id": 5,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Days Since Contract Start",
      "gridPos": {
        "h": 4,
        "w": 3,
        "x": 18,
        "y": 0
      },
      "id": 6,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT CAST(julianday('now') - julianday(current_contract_start_date) AS INTEGER) AS value\nFROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT CAST(julianday('now') - julianday(current_contract_start_date) AS INTEGER) AS value\nFROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "CSM",
      "gridPos": {
        "h": 4,
        "w": 3,
        "x": 21,
        "y": 0
      },
      "id": 7,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "CSE",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 0,
        "y": 4
      },
      "id": 8,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "Sales Executive",
      "gridPos": {
        "h": 4,
        "w": 6,
        "x": 6,
        "y": 4
      },
      "id": 9,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "rawQueryText": "SELECT 'N/A' AS value FROM companies WHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring})) LIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "SM \u2014 Secrets Management",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 8
      },
      "id": 10,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "gauge",
      "title": "SM - Usage %",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 0,
        "y": 9
      },
      "id": 11,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_sm, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_sm, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "SM - Purchased",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 4,
        "y": 9
      },
      "id": 12,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(SUM(CAST(clients_sm AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "rawQueryText": "SELECT COALESCE(SUM(CAST(clients_sm AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "SM - Used",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 8,
        "y": 9
      },
      "id": 13,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'sm'",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'sm'",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "gauge",
      "title": "SM - Usage AVG",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 12,
        "y": 9
      },
      "id": 14,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_sm\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'sm'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_sm) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "rawQueryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_sm\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'sm'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_sm) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      },
      "description": "Average of monthly Utilization % across all months in the selected Time Range filter (not the same as Avg (Last 3M) in the table below)."
    },
    {
      "type": "table",
      "title": "SM Monthly Utilization % by Customer",
      "gridPos": {
        "h": 16,
        "w": 24,
        "x": 0,
        "y": 14
      },
      "id": 15,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_sm, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_sm, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sm' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "desc": true,
            "displayName": "Avg (Last 3M)"
          }
        ]
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "color-background",
              "applyToRow": false,
              "wrapText": true
            },
            "inspect": false,
            "filterable": false,
            "minWidth": 80
          },
          "decimals": 1,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "super-light-red",
                "value": null
              },
              {
                "color": "super-light-blue",
                "value": 30
              },
              {
                "color": "super-light-green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Customer"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              },
              {
                "id": "custom.width",
                "value": 200
              },
              {
                "id": "unit",
                "value": "string"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "text"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg (Last 3M)"
            },
            "properties": [
              {
                "id": "custom.width",
                "value": 110
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "mode": "gradient",
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "CLM \u2014 Certificate Lifecycle Management",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 30
      },
      "id": 16,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "gauge",
      "title": "CLM - Usage %",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 0,
        "y": 31
      },
      "id": 17,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT CASE WHEN CAST(c.clients_ca AS INTEGER) > 0\n  THEN ROUND(100.0 * COALESCE(\n    (\n      SELECT SUM(rcp.amount)\n      FROM report_clients rc\n      JOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id\n      WHERE rc.account_id = c.account_id\n        AND rcp.product = 'ca'\n        AND rc.report_date = (\n          SELECT MAX(rc2.report_date) FROM report_clients rc2\n          JOIN report_clients_product_info rcp2 ON rc2.id = rcp2.report_client_id\n          WHERE rcp2.product = 'ca'\n            AND rc2.account_id = c.account_id\n            AND rc2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n        )\n    ),\n    (\n      SELECT SUM(ro.amount)\n      FROM report_objects ro\n      WHERE ro.account_id = c.account_id\n        AND ro.object_type LIKE '%certificate%'\n        AND ro.report_date = (\n          SELECT MAX(ro2.report_date) FROM report_objects ro2\n          WHERE ro2.account_id = c.account_id\n            AND ro2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n        )\n    ),\n    0\n  ) / CAST(c.clients_ca AS INTEGER), 2)\n  ELSE 0 END AS value\nFROM companies c\nWHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\nLIMIT 1",
          "rawQueryText": "SELECT CASE WHEN CAST(c.clients_ca AS INTEGER) > 0\n  THEN ROUND(100.0 * COALESCE(\n    (\n      SELECT SUM(rcp.amount)\n      FROM report_clients rc\n      JOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id\n      WHERE rc.account_id = c.account_id\n        AND rcp.product = 'ca'\n        AND rc.report_date = (\n          SELECT MAX(rc2.report_date) FROM report_clients rc2\n          JOIN report_clients_product_info rcp2 ON rc2.id = rcp2.report_client_id\n          WHERE rcp2.product = 'ca'\n            AND rc2.account_id = c.account_id\n            AND rc2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n        )\n    ),\n    (\n      SELECT SUM(ro.amount)\n      FROM report_objects ro\n      WHERE ro.account_id = c.account_id\n        AND ro.object_type LIKE '%certificate%'\n        AND ro.report_date = (\n          SELECT MAX(ro2.report_date) FROM report_objects ro2\n          WHERE ro2.account_id = c.account_id\n            AND ro2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n        )\n    ),\n    0\n  ) / CAST(c.clients_ca AS INTEGER), 2)\n  ELSE 0 END AS value\nFROM companies c\nWHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\nLIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "CLM - Purchased",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 4,
        "y": 31
      },
      "id": 18,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(SUM(CAST(clients_ca AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "rawQueryText": "SELECT COALESCE(SUM(CAST(clients_ca AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "CLM - Used",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 8,
        "y": 31
      },
      "id": 19,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(\n  (\n    SELECT SUM(rcp.amount)\n    FROM report_clients rc\n    JOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id\n    WHERE rc.account_id = c.account_id\n      AND rcp.product = 'ca'\n      AND rc.report_date = (\n        SELECT MAX(rc2.report_date) FROM report_clients rc2\n        JOIN report_clients_product_info rcp2 ON rc2.id = rcp2.report_client_id\n        WHERE rcp2.product = 'ca'\n          AND rc2.account_id = c.account_id\n          AND rc2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n      )\n  ),\n  (\n    SELECT SUM(ro.amount)\n    FROM report_objects ro\n    WHERE ro.account_id = c.account_id\n      AND ro.object_type LIKE '%certificate%'\n      AND ro.report_date = (\n        SELECT MAX(ro2.report_date) FROM report_objects ro2\n        WHERE ro2.account_id = c.account_id\n          AND ro2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n      )\n  ),\n  0\n) AS value\nFROM companies c\nWHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\nLIMIT 1",
          "rawQueryText": "SELECT COALESCE(\n  (\n    SELECT SUM(rcp.amount)\n    FROM report_clients rc\n    JOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id\n    WHERE rc.account_id = c.account_id\n      AND rcp.product = 'ca'\n      AND rc.report_date = (\n        SELECT MAX(rc2.report_date) FROM report_clients rc2\n        JOIN report_clients_product_info rcp2 ON rc2.id = rcp2.report_client_id\n        WHERE rcp2.product = 'ca'\n          AND rc2.account_id = c.account_id\n          AND rc2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n      )\n  ),\n  (\n    SELECT SUM(ro.amount)\n    FROM report_objects ro\n    WHERE ro.account_id = c.account_id\n      AND ro.object_type LIKE '%certificate%'\n      AND ro.report_date = (\n        SELECT MAX(ro2.report_date) FROM report_objects ro2\n        WHERE ro2.account_id = c.account_id\n          AND ro2.report_date >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n      )\n  ),\n  0\n) AS value\nFROM companies c\nWHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\nLIMIT 1",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "gauge",
      "title": "CLM - Usage AVG",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 12,
        "y": 31
      },
      "id": 20,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_ca\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'ca'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_ca) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "rawQueryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_ca\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'ca'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_ca) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      },
      "description": "Average of monthly Utilization % across all months in the selected Time Range filter (not the same as Avg (Last 3M) in the table below)."
    },
    {
      "type": "table",
      "title": "CLM Monthly Utilization % by Customer",
      "gridPos": {
        "h": 16,
        "w": 24,
        "x": 0,
        "y": 36
      },
      "id": 21,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_ca, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nCertificateDaily AS (\n    SELECT\n        cd.company_name,\n        cd.account_key,\n        cd.clients_purchased,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        date(r.report_date) AS report_date,\n        CASE\n    WHEN (COALESCE(r.ca_self_signed, 0) - COALESCE(r.risk_expired, 0)) < 0 THEN 0\n    ELSE (COALESCE(r.ca_self_signed, 0) - COALESCE(r.risk_expired, 0))\nEND AS daily_certs\n    FROM CompanyData cd\n    INNER JOIN reports r ON LOWER(r.account_id) = cd.account_key\n    WHERE r.report_type = 'certificate_analysis'\n      AND date(r.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(r.report_date) <= date(strftime('%Y-%m-%d', date('now', 'start of month', '-1 day')))\n),\nDailyMax AS (\n    SELECT company_name, account_key, clients_purchased, report_month, report_date,\n        MAX(daily_certs) AS daily_max\n    FROM CertificateDaily\n    GROUP BY company_name, account_key, clients_purchased, report_month, report_date\n),\nMonthlyMax AS (\n    SELECT company_name, account_key, clients_purchased, report_month,\n        MAX(daily_max) AS product_used\n    FROM DailyMax\n    GROUP BY company_name, account_key, clients_purchased, report_month\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total\n    FROM MonthlyMax\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(product_used_total * 100.0 / clients_purchased, 1)\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_ca, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nCertificateDaily AS (\n    SELECT\n        cd.company_name,\n        cd.account_key,\n        cd.clients_purchased,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        date(r.report_date) AS report_date,\n        CASE\n    WHEN (COALESCE(r.ca_self_signed, 0) - COALESCE(r.risk_expired, 0)) < 0 THEN 0\n    ELSE (COALESCE(r.ca_self_signed, 0) - COALESCE(r.risk_expired, 0))\nEND AS daily_certs\n    FROM CompanyData cd\n    INNER JOIN reports r ON LOWER(r.account_id) = cd.account_key\n    WHERE r.report_type = 'certificate_analysis'\n      AND date(r.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(r.report_date) <= date(strftime('%Y-%m-%d', date('now', 'start of month', '-1 day')))\n),\nDailyMax AS (\n    SELECT company_name, account_key, clients_purchased, report_month, report_date,\n        MAX(daily_certs) AS daily_max\n    FROM CertificateDaily\n    GROUP BY company_name, account_key, clients_purchased, report_month, report_date\n),\nMonthlyMax AS (\n    SELECT company_name, account_key, clients_purchased, report_month,\n        MAX(daily_max) AS product_used\n    FROM DailyMax\n    GROUP BY company_name, account_key, clients_purchased, report_month\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total\n    FROM MonthlyMax\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(product_used_total * 100.0 / clients_purchased, 1)\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "desc": true,
            "displayName": "Avg (Last 3M)"
          }
        ]
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "color-background",
              "applyToRow": false,
              "wrapText": true
            },
            "inspect": false,
            "filterable": false,
            "minWidth": 80
          },
          "decimals": 1,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "super-light-red",
                "value": null
              },
              {
                "color": "super-light-blue",
                "value": 30
              },
              {
                "color": "super-light-green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Customer"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              },
              {
                "id": "custom.width",
                "value": 200
              },
              {
                "id": "unit",
                "value": "string"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "text"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg (Last 3M)"
            },
            "properties": [
              {
                "id": "custom.width",
                "value": 110
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "mode": "gradient",
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "SRA \u2014 Secure Remote Access",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 52
      },
      "id": 22,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "gauge",
      "title": "SRA - Usage %",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 0,
        "y": 53
      },
      "id": 23,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_sra, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_sra, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "SRA - Purchased",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 4,
        "y": 53
      },
      "id": 24,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(SUM(CAST(clients_sra AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "rawQueryText": "SELECT COALESCE(SUM(CAST(clients_sra AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "SRA - Used",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 8,
        "y": 53
      },
      "id": 25,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'sra'",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'sra'",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "gauge",
      "title": "SRA - Usage AVG",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 12,
        "y": 53
      },
      "id": 26,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_sra\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'sra'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_sra) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "rawQueryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_sra\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'sra'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_sra) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      },
      "description": "Average of monthly Utilization % across all months in the selected Time Range filter (not the same as Avg (Last 3M) in the table below)."
    },
    {
      "type": "table",
      "title": "SRA Monthly Utilization % by Customer",
      "gridPos": {
        "h": 16,
        "w": 24,
        "x": 0,
        "y": 58
      },
      "id": 27,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_sra, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_sra, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'sra' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "desc": true,
            "displayName": "Avg (Last 3M)"
          }
        ]
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "color-background",
              "applyToRow": false,
              "wrapText": true
            },
            "inspect": false,
            "filterable": false,
            "minWidth": 80
          },
          "decimals": 1,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "super-light-red",
                "value": null
              },
              {
                "color": "super-light-blue",
                "value": 30
              },
              {
                "color": "super-light-green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Customer"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              },
              {
                "id": "custom.width",
                "value": 200
              },
              {
                "id": "unit",
                "value": "string"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "text"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg (Last 3M)"
            },
            "properties": [
              {
                "id": "custom.width",
                "value": 110
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "mode": "gradient",
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "PWM \u2014 Password Management",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 74
      },
      "id": 28,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "gauge",
      "title": "PWM - Usage %",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 0,
        "y": 75
      },
      "id": 29,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_pm, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key, CAST(COALESCE(c.clients_pm, '0') AS INTEGER) AS purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nLatestUsage AS (\n    SELECT cd.account_key, cd.purchased,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.amount ELSE 0 END), 0) AS used_clients\n    FROM CompanyData cd\n    INNER JOIN LatestReportId lr ON lr.account_key = cd.account_key\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY cd.account_key, cd.purchased\n)\nSELECT CASE WHEN SUM(lu.purchased) > 0 THEN ROUND(100.0 * SUM(lu.used_clients) / SUM(lu.purchased), 2) ELSE 0 END AS value\nFROM LatestUsage lu",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "PWM - Purchased",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 4,
        "y": 75
      },
      "id": 30,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT COALESCE(SUM(CAST(clients_pm AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "rawQueryText": "SELECT COALESCE(SUM(CAST(clients_pm AS INTEGER)), 0) AS value\nFROM companies\nWHERE account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR companies.name IN (${company_name:sqlstring}))",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "stat",
      "title": "PWM - Used",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 8,
        "y": 75
      },
      "id": 31,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'apm'",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT LOWER(c.account_id) AS account_key\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring}))\n),\nOneReportPerAccountDate AS (\n    SELECT LOWER(account_id) AS account_key, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports WHERE report_type = 'ObjectsReport'\n    GROUP BY LOWER(account_id), date(report_date)\n),\nLatestReportPerAccount AS (\n    SELECT cd.account_key, MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= date(\n        CASE\n            WHEN '${period_months}' = 'From Contract Start' THEN\n                COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n            WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n                COALESCE(\n                    CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                        CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                            THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                            ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                        END\n                    ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n                    END,\n                    strftime('%Y-%m-%d', 'now', 'start of month', '-11 months'))\n            WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n            ELSE strftime('%Y-%m-%d', 'now', printf('-%d months', CASE\n        WHEN '${period_months}' = 'Current Month' THEN 1\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN 3\n        WHEN '${period_months}' = 'Last 6 Months' THEN 6\n        WHEN '${period_months}' = 'Last 9 Months' THEN 9\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN 12\n        WHEN '${period_months}' = 'Last 24 Months' THEN 24\n        WHEN '${period_months}' = 'From Contract Start' THEN CAST((\n            julianday('now') - julianday(COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months')))\n        ) / 30.44 AS INTEGER)\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN 12\n        ELSE 12\n    END))\n        END\n    )\n    GROUP BY cd.account_key\n),\nLatestReportId AS (\n    SELECT lra.account_key, MAX(orad.report_id) AS report_id\n    FROM LatestReportPerAccount lra\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = lra.account_key AND orad.report_date = lra.latest_report_date\n    GROUP BY lra.account_key\n)\n\nSELECT COALESCE(SUM(rcp.amount), 0) AS value\nFROM LatestReportId lr\nJOIN report_clients rc ON rc.report_id = lr.report_id\nJOIN report_clients_product_info rcp ON rc.id = rcp.report_client_id AND rcp.product = 'apm'",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "colorMode": "none",
        "graphMode": "none",
        "justifyMode": "center",
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "textMode": "auto"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          },
          "mappings": []
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "gauge",
      "title": "PWM - Usage AVG",
      "gridPos": {
        "h": 5,
        "w": 4,
        "x": 12,
        "y": 75
      },
      "id": 32,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_pm\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'apm'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_pm) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "rawQueryText": "SELECT ROUND(AVG(\"Utilization %\"), 1) AS value FROM (-- Legacy monthly utilization % (used only, latest report per month).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id\n    FROM companies\n    WHERE name = '${company_name}'\n      AND account_id IS NOT NULL AND account_id != ''\n      AND account_id = '${account_id}'\n),\nAccountHubSpotData AS (\n    SELECT ca.account_id, c.clients_pm\n    FROM CompanyAccounts ca\n    INNER JOIN companies c ON ca.account_id = c.account_id\n    WHERE c.name = '${company_name}'\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n            WHEN '${period_months}' LIKE '3%' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n            WHEN '${period_months}' LIKE '12%' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n            ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n        END AS range_start_date,\n        strftime('%Y-%m-%d', 'now') AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthsWithData AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', r.report_date) AS report_month,\n        strftime('%Y-%m-01', r.report_date) AS month_start_date,\n        date(strftime('%Y-%m-01', r.report_date), '+1 month', '-1 day') AS month_end_date\n    FROM AccountDateRanges adr\n    INNER JOIN reports r ON r.account_id = adr.account_id AND r.report_type = 'ObjectsReport'\n    WHERE date(r.report_date) >= date(adr.range_start_date)\n      AND date(r.report_date) <= date(adr.range_end_date)\n),\nOneReportPerAccountDate AS (\n    SELECT account_id, date(report_date) AS report_date, MAX(id) AS report_id\n    FROM reports\n    WHERE report_type = 'ObjectsReport'\n      AND account_id IN (SELECT account_id FROM CompanyAccounts)\n    GROUP BY account_id, date(report_date)\n),\nLatestReportDatePerMonth AS (\n    SELECT\n        LOWER(m.account_id) AS account_key,\n        m.report_month,\n        m.month_start_date,\n        m.month_end_date,\n        MAX(r.report_date) AS latest_report_date\n    FROM MonthsWithData m\n    INNER JOIN reports r ON r.account_id = m.account_id AND r.report_type = 'ObjectsReport'\n        AND date(r.report_date) >= m.month_start_date\n        AND date(r.report_date) <= m.month_end_date\n    GROUP BY LOWER(m.account_id), m.report_month, m.month_start_date, m.month_end_date\n),\nSingleReportId AS (\n    SELECT\n        l.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM LatestReportDatePerMonth l\n    INNER JOIN OneReportPerAccountDate orad\n        ON LOWER(orad.account_id) = l.account_key\n        AND orad.report_date = date(l.latest_report_date)\n    GROUP BY l.report_month, l.latest_report_date\n),\nMonthlyUsed AS (\n    SELECT\n        s.report_month,\n        COALESCE(SUM(rcp.amount), 0) AS used_amount\n    FROM SingleReportId s\n    LEFT JOIN report_clients rc ON rc.report_id = s.report_id\n    LEFT JOIN report_clients_product_info rcp\n        ON rc.id = rcp.report_client_id AND rcp.product = 'apm'\n    GROUP BY s.report_month\n)\nSELECT\n    mu.report_month AS \"Month\",\n    ROUND(\n        100.0 * mu.used_amount / NULLIF(CAST(MAX(ahsd.clients_pm) AS INTEGER), 0),\n        1\n    ) AS \"Utilization %\"\nFROM MonthlyUsed mu\nCROSS JOIN AccountHubSpotData ahsd\nGROUP BY mu.report_month, mu.used_amount\nORDER BY mu.report_month)",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "minVizHeight": 75,
        "minVizWidth": 75,
        "orientation": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "/.*/",
          "values": true
        },
        "showThresholdLabels": false,
        "showThresholdMarkers": true
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "max": 100,
          "min": 0,
          "noValue": "0%",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "red",
                "value": null
              },
              {
                "color": "#5794F2",
                "value": 30
              },
              {
                "color": "green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      },
      "description": "Average of monthly Utilization % across all months in the selected Time Range filter (not the same as Avg (Last 3M) in the table below)."
    },
    {
      "type": "table",
      "title": "PWM Monthly Utilization % by Customer",
      "gridPos": {
        "h": 16,
        "w": 24,
        "x": 0,
        "y": 80
      },
      "id": 33,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_pm, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "rawQueryText": "WITH CompanyData AS (\n    SELECT DISTINCT c.name AS company_name, LOWER(c.account_id) AS account_key,\n        CAST(COALESCE(c.clients_pm, '0') AS INTEGER) AS clients_purchased\n    FROM companies c\n    WHERE c.account_id IS NOT NULL AND c.account_id != '' AND ('${company_name:raw}' IN ('All', '%', '$__all') OR c.name IN (${company_name:sqlstring})) AND ('${account_id:raw}' IN ('All', '%', '$__all') OR c.account_id IN (${account_id:sqlstring}))\n),\nDateRange AS (\n    SELECT CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start\n),\nOneReportPerAccountDate AS (\n    SELECT cd.account_key, date(r.report_date) AS report_date, MAX(r.id) AS report_id\n    FROM reports r\n    INNER JOIN CompanyData cd ON cd.account_key = LOWER(r.account_id)\n    WHERE r.report_type = 'ObjectsReport'\n    GROUP BY cd.account_key, date(r.report_date)\n),\nMonthlyLatest AS (\n    SELECT cd.company_name, cd.account_key, cd.clients_purchased,\n        strftime('%Y-%m', orad.report_date) AS report_month,\n        MAX(orad.report_date) AS latest_report_date\n    FROM CompanyData cd\n    INNER JOIN OneReportPerAccountDate orad ON orad.account_key = cd.account_key\n    WHERE date(orad.report_date) >= (SELECT range_start FROM DateRange)\n      AND date(orad.report_date) <= date(strftime('%Y-%m-%d', 'now'))\n    GROUP BY cd.company_name, cd.account_key, cd.clients_purchased, strftime('%Y-%m', orad.report_date)\n),\nLatestReportId AS (\n    SELECT ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month,\n        MAX(orad.report_id) AS report_id\n    FROM MonthlyLatest ml\n    INNER JOIN OneReportPerAccountDate orad\n        ON orad.account_key = ml.account_key AND orad.report_date = ml.latest_report_date\n    GROUP BY ml.company_name, ml.account_key, ml.clients_purchased, ml.report_month\n),\nMonthlyUsage AS (\n    SELECT lr.company_name, lr.clients_purchased, lr.report_month,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.amount ELSE 0 END), 0) AS product_used,\n        COALESCE(SUM(CASE WHEN rcp.product = 'apm' THEN rcp.exceeded_amount ELSE 0 END), 0) AS product_exceeded\n    FROM LatestReportId lr\n    LEFT JOIN report_clients rc ON rc.report_id = lr.report_id\n    LEFT JOIN report_clients_product_info rcp ON rcp.report_client_id = rc.id\n    GROUP BY lr.company_name, lr.clients_purchased, lr.report_month, lr.report_id\n),\nAggregatedByCompany AS (\n    SELECT company_name, SUM(clients_purchased) AS clients_purchased, report_month,\n        SUM(product_used) AS product_used_total,\n        SUM(product_exceeded) AS product_exceeded_total\n    FROM MonthlyUsage\n    GROUP BY company_name, report_month\n),\nFinalData AS (\n    SELECT company_name, report_month,\n        CASE\n            WHEN clients_purchased <= 0 THEN NULL\n            WHEN clients_purchased IN (999, 99999) THEN NULL\n            ELSE ROUND(\n                (product_used_total + product_exceeded_total) * 100.0 / clients_purchased,\n                1\n            )\n        END AS util_pct\n    FROM AggregatedByCompany\n)\nSELECT\n    company_name AS \"Customer\",\n    ROUND(AVG(CASE WHEN report_month IN ('2026-07', '2026-08', '2026-09') THEN util_pct END), 1) AS \"Avg (Last 3M)\",\n    MAX(CASE WHEN report_month = '2024-10' THEN util_pct END) AS \"Oct-24\", MAX(CASE WHEN report_month = '2024-11' THEN util_pct END) AS \"Nov-24\", MAX(CASE WHEN report_month = '2024-12' THEN util_pct END) AS \"Dec-24\", MAX(CASE WHEN report_month = '2025-01' THEN util_pct END) AS \"Jan-25\", MAX(CASE WHEN report_month = '2025-02' THEN util_pct END) AS \"Feb-25\", MAX(CASE WHEN report_month = '2025-03' THEN util_pct END) AS \"Mar-25\", MAX(CASE WHEN report_month = '2025-04' THEN util_pct END) AS \"Apr-25\", MAX(CASE WHEN report_month = '2025-05' THEN util_pct END) AS \"May-25\", MAX(CASE WHEN report_month = '2025-06' THEN util_pct END) AS \"Jun-25\", MAX(CASE WHEN report_month = '2025-07' THEN util_pct END) AS \"Jul-25\", MAX(CASE WHEN report_month = '2025-08' THEN util_pct END) AS \"Aug-25\", MAX(CASE WHEN report_month = '2025-09' THEN util_pct END) AS \"Sep-25\", MAX(CASE WHEN report_month = '2025-10' THEN util_pct END) AS \"Oct-25\", MAX(CASE WHEN report_month = '2025-11' THEN util_pct END) AS \"Nov-25\", MAX(CASE WHEN report_month = '2025-12' THEN util_pct END) AS \"Dec-25\", MAX(CASE WHEN report_month = '2026-01' THEN util_pct END) AS \"Jan-26\", MAX(CASE WHEN report_month = '2026-02' THEN util_pct END) AS \"Feb-26\", MAX(CASE WHEN report_month = '2026-03' THEN util_pct END) AS \"Mar-26\", MAX(CASE WHEN report_month = '2026-04' THEN util_pct END) AS \"Apr-26\", MAX(CASE WHEN report_month = '2026-05' THEN util_pct END) AS \"May-26\", MAX(CASE WHEN report_month = '2026-06' THEN util_pct END) AS \"Jun-26\", MAX(CASE WHEN report_month = '2026-07' THEN util_pct END) AS \"Jul-26\", MAX(CASE WHEN report_month = '2026-08' THEN util_pct END) AS \"Aug-26\", MAX(CASE WHEN report_month = '2026-09' THEN util_pct END) AS \"Sep-26\"\nFROM FinalData\nGROUP BY company_name\nHAVING COUNT(util_pct) > 0\nORDER BY \"Avg (Last 3M)\" DESC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "desc": true,
            "displayName": "Avg (Last 3M)"
          }
        ]
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "color-background",
              "applyToRow": false,
              "wrapText": true
            },
            "inspect": false,
            "filterable": false,
            "minWidth": 80
          },
          "decimals": 1,
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "super-light-red",
                "value": null
              },
              {
                "color": "super-light-blue",
                "value": 30
              },
              {
                "color": "super-light-green",
                "value": 70
              }
            ]
          },
          "unit": "percent"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Customer"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "auto",
                  "wrapText": true
                }
              },
              {
                "id": "custom.width",
                "value": 200
              },
              {
                "id": "unit",
                "value": "string"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "text"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg (Last 3M)"
            },
            "properties": [
              {
                "id": "custom.width",
                "value": 110
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "mode": "gradient",
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "Objects Info",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 96
      },
      "id": 34,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "barchart",
      "title": "Objects info",
      "description": "Stacked monthly object counts from latest report per month. Legend shows last count per type.",
      "gridPos": {
        "h": 18,
        "w": 24,
        "x": 0,
        "y": 97
      },
      "id": 35,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "-- Objects stacked bar: latest report per month, wide object-type columns (matches production dashboard).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id, name AS company_name\n    FROM companies\n    WHERE account_id IS NOT NULL AND account_id != ''\n      AND (\n        '${company_name:raw}' IN ('All', '%', '$__all')\n        OR name IN (${company_name:sqlstring})\n        OR name = '${company_name}'\n      )\n      AND (\n        '${account_id:raw}' IN ('All', '%', '$__all')\n        OR LOWER('${account_id:raw}') = 'all'\n        OR account_id = '${account_id}'\n      )\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start_date,\n        CASE\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '+1 year')\n                END\n            ELSE strftime('%Y-%m-%d', 'now')\n            END\n        ELSE strftime('%Y-%m-%d', 'now')\n    END AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthlyDates AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', ro.report_date) AS report_month,\n        strftime('%Y-%m-01', ro.report_date) AS month_start_date\n    FROM AccountDateRanges adr\n    INNER JOIN report_objects ro ON ro.account_id = adr.account_id\n    WHERE date(ro.report_date) >= adr.range_start_date\n      AND date(ro.report_date) <= adr.range_end_date\n),\nLatestReportPerMonth AS (\n    SELECT\n        md.account_id,\n        md.report_month,\n        md.month_start_date,\n        MAX(ro.report_date) AS latest_report_date\n    FROM MonthlyDates md\n    INNER JOIN report_objects ro ON ro.account_id = md.account_id\n        AND strftime('%Y-%m', ro.report_date) = md.report_month\n    GROUP BY md.account_id, md.report_month, md.month_start_date\n),\nMonthlyObjectData AS (\n    SELECT\n        lrpm.account_id,\n        lrpm.report_month,\n        ro.object_type,\n        SUM(ro.amount) AS amount\n    FROM LatestReportPerMonth lrpm\n    INNER JOIN report_objects ro ON ro.account_id = lrpm.account_id\n        AND ro.report_date = lrpm.latest_report_date\n    WHERE ro.amount > 0\n    GROUP BY lrpm.account_id, lrpm.report_month, ro.object_type\n)\nSELECT\n    mod.report_month AS month,\n    SUM(CASE WHEN mod.object_type LIKE '%access_role%' AND mod.object_type NOT LIKE '%access_role_rules%' THEN mod.amount ELSE 0 END) AS \"Access Role\",\n    SUM(CASE WHEN mod.object_type LIKE '%aes_key%' THEN mod.amount ELSE 0 END) AS \"AES Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method%' AND mod.object_type NOT LIKE '%auth_method_%' THEN mod.amount ELSE 0 END) AS \"Auth Method\",\n    SUM(CASE WHEN mod.object_type LIKE '%certified_automation%' THEN mod.amount ELSE 0 END) AS \"Certified Automation\",\n    SUM(CASE WHEN mod.object_type LIKE '%classic_key%' THEN mod.amount ELSE 0 END) AS \"Classic Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%code_sign%' THEN mod.amount ELSE 0 END) AS \"Code Sign\",\n    SUM(CASE WHEN mod.object_type LIKE '%document_sign%' THEN mod.amount ELSE 0 END) AS \"Document Sign\",\n    SUM(CASE WHEN mod.object_type LIKE '%dynamic_secret%' THEN mod.amount ELSE 0 END) AS \"Dynamic Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%pki_cert_issuer%' THEN mod.amount ELSE 0 END) AS \"PKI Cert Issuer\",\n    SUM(CASE WHEN mod.object_type LIKE '%rotated_secret%' THEN mod.amount ELSE 0 END) AS \"Rotated Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%rsa_key%' THEN mod.amount ELSE 0 END) AS \"RSA Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%ssh_cert_issuer%' THEN mod.amount ELSE 0 END) AS \"SSH Cert Issuer\",\n    SUM(CASE WHEN mod.object_type LIKE '%static_secret%' THEN mod.amount ELSE 0 END) AS \"Static Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%sub_claim%' THEN mod.amount ELSE 0 END) AS \"Sub Claim\",\n    SUM(CASE WHEN mod.object_type LIKE '%target%' THEN mod.amount ELSE 0 END) AS \"Target\",\n    SUM(CASE WHEN mod.object_type LIKE '%tokenizer%' THEN mod.amount ELSE 0 END) AS \"Tokenizer\",\n    SUM(CASE WHEN mod.object_type LIKE '%noti_forwarder%' THEN mod.amount ELSE 0 END) AS \"Notification Forwarder\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_aws_iam%' THEN mod.amount ELSE 0 END) AS \"Auth Method AWS IAM\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_api_key%' THEN mod.amount ELSE 0 END) AS \"Auth Method API Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_kubernetes%' THEN mod.amount ELSE 0 END) AS \"Auth Method Kubernetes\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_saml%' THEN mod.amount ELSE 0 END) AS \"Auth Method SAML\",\n    SUM(CASE WHEN mod.object_type LIKE '%certificate%' THEN mod.amount ELSE 0 END) AS \"Certificate\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_azure_ad%' THEN mod.amount ELSE 0 END) AS \"Auth Method Azure AD\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_universal_identity%' THEN mod.amount ELSE 0 END) AS \"Auth Method Universal Identity\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_jwt%' THEN mod.amount ELSE 0 END) AS \"Auth Method JWT\",\n    SUM(CASE WHEN mod.object_type LIKE '%dfc_key%' THEN mod.amount ELSE 0 END) AS \"DFC Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_email_pass%' THEN mod.amount ELSE 0 END) AS \"Auth Method Email/Pass\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_oidc%' THEN mod.amount ELSE 0 END) AS \"Auth Method OIDC\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_ldap%' THEN mod.amount ELSE 0 END) AS \"Auth Method LDAP\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_gcp%' THEN mod.amount ELSE 0 END) AS \"Auth Method GCP\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_cert%' THEN mod.amount ELSE 0 END) AS \"Auth Method Cert\",\n    SUM(CASE WHEN mod.object_type LIKE '%transaction_per_client%' THEN mod.amount ELSE 0 END) AS \"Transaction Per Client\",\n    SUM(CASE WHEN mod.object_type LIKE '%transactions_per_client_minutes%' THEN mod.amount ELSE 0 END) AS \"Transactions Per Client Minutes\",\n    SUM(CASE WHEN mod.object_type LIKE '%multi_cloud_kms%' THEN mod.amount ELSE 0 END) AS \"Multi Cloud KMS\",\n    SUM(CASE WHEN mod.object_type LIKE '%external_secrets_manager%' THEN mod.amount ELSE 0 END) AS \"External Secrets Manager\",\n    SUM(CASE WHEN mod.object_type LIKE '%transactions_per_account_daily%' THEN mod.amount ELSE 0 END) AS \"Transactions Per Account Daily\",\n    SUM(CASE WHEN mod.object_type LIKE '%oidc_client%' THEN mod.amount ELSE 0 END) AS \"OIDC Client\",\n    SUM(CASE WHEN mod.object_type LIKE '%group%' THEN mod.amount ELSE 0 END) AS \"Group\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_oci%' THEN mod.amount ELSE 0 END) AS \"Auth Method OCI\"\nFROM MonthlyObjectData mod\nINNER JOIN CompanyAccounts ca ON mod.account_id = ca.account_id\nGROUP BY mod.report_month\nORDER BY mod.report_month",
          "rawQueryText": "-- Objects stacked bar: latest report per month, wide object-type columns (matches production dashboard).\nWITH CompanyAccounts AS (\n    SELECT DISTINCT account_id, name AS company_name\n    FROM companies\n    WHERE account_id IS NOT NULL AND account_id != ''\n      AND (\n        '${company_name:raw}' IN ('All', '%', '$__all')\n        OR name IN (${company_name:sqlstring})\n        OR name = '${company_name}'\n      )\n      AND (\n        '${account_id:raw}' IN ('All', '%', '$__all')\n        OR LOWER('${account_id:raw}') = 'all'\n        OR account_id = '${account_id}'\n      )\n),\nAccountDateRanges AS (\n    SELECT ca.account_id,\n        CASE\n        WHEN '${period_months}' = 'Current Month' THEN strftime('%Y-%m-%d', 'now', 'start of month')\n        WHEN '${period_months}' = 'Last 3 Months' OR '${period_months}' LIKE '3 Months' THEN strftime('%Y-%m-%d', 'now', '-3 months')\n        WHEN '${period_months}' = 'Last 6 Months' THEN strftime('%Y-%m-%d', 'now', '-6 months')\n        WHEN '${period_months}' = 'Last 9 Months' THEN strftime('%Y-%m-%d', 'now', '-9 months')\n        WHEN '${period_months}' = 'Last 12 Months' OR '${period_months}' LIKE '12 Months' THEN strftime('%Y-%m-%d', 'now', '-12 months')\n        WHEN '${period_months}' = 'Last 24 Months' THEN strftime('%Y-%m-%d', 'now', '-24 months')\n        WHEN '${period_months}' = 'From Contract Start' THEN\n            COALESCE((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), strftime('%Y-%m-%d', 'now', '-12 months'))\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '-1 year')\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                END\n            ELSE strftime('%Y-%m-%d', 'now', 'start of month', '-11 months')\n            END\n        ELSE strftime('%Y-%m-%d', 'now', '-12 months')\n    END AS range_start_date,\n        CASE\n        WHEN '${period_months}' = 'From Contract Start Last 12 Months' THEN\n            CASE WHEN (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) IS NOT NULL AND (SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)) != '' THEN\n                CASE WHEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10))) > date('now')\n                    THEN date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)))\n                    ELSE date(strftime('%Y', 'now') || '-' || strftime('%m-%d', substr((SELECT MIN(substr(c.current_contract_start_date, 1, 10))\n        FROM companies c\n        WHERE c.current_contract_start_date IS NOT NULL AND c.current_contract_start_date != ''\n          AND c.account_id IS NOT NULL AND c.account_id != ''\n          AND (\n    '${company_name:raw}' IN ('All', '%', '$__all')\n    OR name IN (${company_name:sqlstring})\n    OR name = '${company_name}'\n)), 1, 10)), '+1 year')\n                END\n            ELSE strftime('%Y-%m-%d', 'now')\n            END\n        ELSE strftime('%Y-%m-%d', 'now')\n    END AS range_end_date\n    FROM CompanyAccounts ca\n),\nMonthlyDates AS (\n    SELECT DISTINCT\n        adr.account_id,\n        strftime('%Y-%m', ro.report_date) AS report_month,\n        strftime('%Y-%m-01', ro.report_date) AS month_start_date\n    FROM AccountDateRanges adr\n    INNER JOIN report_objects ro ON ro.account_id = adr.account_id\n    WHERE date(ro.report_date) >= adr.range_start_date\n      AND date(ro.report_date) <= adr.range_end_date\n),\nLatestReportPerMonth AS (\n    SELECT\n        md.account_id,\n        md.report_month,\n        md.month_start_date,\n        MAX(ro.report_date) AS latest_report_date\n    FROM MonthlyDates md\n    INNER JOIN report_objects ro ON ro.account_id = md.account_id\n        AND strftime('%Y-%m', ro.report_date) = md.report_month\n    GROUP BY md.account_id, md.report_month, md.month_start_date\n),\nMonthlyObjectData AS (\n    SELECT\n        lrpm.account_id,\n        lrpm.report_month,\n        ro.object_type,\n        SUM(ro.amount) AS amount\n    FROM LatestReportPerMonth lrpm\n    INNER JOIN report_objects ro ON ro.account_id = lrpm.account_id\n        AND ro.report_date = lrpm.latest_report_date\n    WHERE ro.amount > 0\n    GROUP BY lrpm.account_id, lrpm.report_month, ro.object_type\n)\nSELECT\n    mod.report_month AS month,\n    SUM(CASE WHEN mod.object_type LIKE '%access_role%' AND mod.object_type NOT LIKE '%access_role_rules%' THEN mod.amount ELSE 0 END) AS \"Access Role\",\n    SUM(CASE WHEN mod.object_type LIKE '%aes_key%' THEN mod.amount ELSE 0 END) AS \"AES Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method%' AND mod.object_type NOT LIKE '%auth_method_%' THEN mod.amount ELSE 0 END) AS \"Auth Method\",\n    SUM(CASE WHEN mod.object_type LIKE '%certified_automation%' THEN mod.amount ELSE 0 END) AS \"Certified Automation\",\n    SUM(CASE WHEN mod.object_type LIKE '%classic_key%' THEN mod.amount ELSE 0 END) AS \"Classic Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%code_sign%' THEN mod.amount ELSE 0 END) AS \"Code Sign\",\n    SUM(CASE WHEN mod.object_type LIKE '%document_sign%' THEN mod.amount ELSE 0 END) AS \"Document Sign\",\n    SUM(CASE WHEN mod.object_type LIKE '%dynamic_secret%' THEN mod.amount ELSE 0 END) AS \"Dynamic Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%pki_cert_issuer%' THEN mod.amount ELSE 0 END) AS \"PKI Cert Issuer\",\n    SUM(CASE WHEN mod.object_type LIKE '%rotated_secret%' THEN mod.amount ELSE 0 END) AS \"Rotated Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%rsa_key%' THEN mod.amount ELSE 0 END) AS \"RSA Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%ssh_cert_issuer%' THEN mod.amount ELSE 0 END) AS \"SSH Cert Issuer\",\n    SUM(CASE WHEN mod.object_type LIKE '%static_secret%' THEN mod.amount ELSE 0 END) AS \"Static Secret\",\n    SUM(CASE WHEN mod.object_type LIKE '%sub_claim%' THEN mod.amount ELSE 0 END) AS \"Sub Claim\",\n    SUM(CASE WHEN mod.object_type LIKE '%target%' THEN mod.amount ELSE 0 END) AS \"Target\",\n    SUM(CASE WHEN mod.object_type LIKE '%tokenizer%' THEN mod.amount ELSE 0 END) AS \"Tokenizer\",\n    SUM(CASE WHEN mod.object_type LIKE '%noti_forwarder%' THEN mod.amount ELSE 0 END) AS \"Notification Forwarder\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_aws_iam%' THEN mod.amount ELSE 0 END) AS \"Auth Method AWS IAM\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_api_key%' THEN mod.amount ELSE 0 END) AS \"Auth Method API Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_kubernetes%' THEN mod.amount ELSE 0 END) AS \"Auth Method Kubernetes\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_saml%' THEN mod.amount ELSE 0 END) AS \"Auth Method SAML\",\n    SUM(CASE WHEN mod.object_type LIKE '%certificate%' THEN mod.amount ELSE 0 END) AS \"Certificate\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_azure_ad%' THEN mod.amount ELSE 0 END) AS \"Auth Method Azure AD\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_universal_identity%' THEN mod.amount ELSE 0 END) AS \"Auth Method Universal Identity\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_jwt%' THEN mod.amount ELSE 0 END) AS \"Auth Method JWT\",\n    SUM(CASE WHEN mod.object_type LIKE '%dfc_key%' THEN mod.amount ELSE 0 END) AS \"DFC Key\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_email_pass%' THEN mod.amount ELSE 0 END) AS \"Auth Method Email/Pass\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_oidc%' THEN mod.amount ELSE 0 END) AS \"Auth Method OIDC\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_ldap%' THEN mod.amount ELSE 0 END) AS \"Auth Method LDAP\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_gcp%' THEN mod.amount ELSE 0 END) AS \"Auth Method GCP\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_cert%' THEN mod.amount ELSE 0 END) AS \"Auth Method Cert\",\n    SUM(CASE WHEN mod.object_type LIKE '%transaction_per_client%' THEN mod.amount ELSE 0 END) AS \"Transaction Per Client\",\n    SUM(CASE WHEN mod.object_type LIKE '%transactions_per_client_minutes%' THEN mod.amount ELSE 0 END) AS \"Transactions Per Client Minutes\",\n    SUM(CASE WHEN mod.object_type LIKE '%multi_cloud_kms%' THEN mod.amount ELSE 0 END) AS \"Multi Cloud KMS\",\n    SUM(CASE WHEN mod.object_type LIKE '%external_secrets_manager%' THEN mod.amount ELSE 0 END) AS \"External Secrets Manager\",\n    SUM(CASE WHEN mod.object_type LIKE '%transactions_per_account_daily%' THEN mod.amount ELSE 0 END) AS \"Transactions Per Account Daily\",\n    SUM(CASE WHEN mod.object_type LIKE '%oidc_client%' THEN mod.amount ELSE 0 END) AS \"OIDC Client\",\n    SUM(CASE WHEN mod.object_type LIKE '%group%' THEN mod.amount ELSE 0 END) AS \"Group\",\n    SUM(CASE WHEN mod.object_type LIKE '%auth_method_oci%' THEN mod.amount ELSE 0 END) AS \"Auth Method OCI\"\nFROM MonthlyObjectData mod\nINNER JOIN CompanyAccounts ca ON mod.account_id = ca.account_id\nGROUP BY mod.report_month\nORDER BY mod.report_month",
          "queryType": "table",
          "timeColumns": [
            "time",
            "ts"
          ],
          "refId": "A"
        }
      ],
      "options": {
        "barRadius": 0,
        "barWidth": 0.97,
        "fullHighlight": false,
        "groupWidth": 0.7,
        "legend": {
          "calcs": [
            "last"
          ],
          "displayMode": "list",
          "placement": "right",
          "showLegend": true
        },
        "orientation": "auto",
        "showValue": "never",
        "stacking": "normal",
        "tooltip": {
          "mode": "single",
          "sort": "none"
        },
        "xField": "month",
        "xTickLabelRotation": 0,
        "xTickLabelSpacing": 0
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "axisBorderShow": false,
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "fillOpacity": 80,
            "gradientMode": "none",
            "hideFrom": {
              "legend": false,
              "tooltip": false,
              "viz": false
            },
            "lineWidth": 1,
            "scaleDistribution": {
              "type": "linear"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "red",
                "value": 80
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Purchased Limit"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "yellow",
                  "mode": "fixed"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Certificate"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "blue",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "row",
      "title": "Portfolio Anomalies",
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 115
      },
      "id": 36,
      "collapsed": false,
      "panels": []
    },
    {
      "type": "table",
      "title": "Under-Adopted Accounts (SM < 30%)",
      "gridPos": {
        "h": 8,
        "w": 24,
        "x": 0,
        "y": 116
      },
      "id": 37,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT c.name AS \"Customer\", c.account_id AS \"Account ID\",\n  c.clients_sm AS \"SM Purchased\", r.clients_sm_total_amount AS \"SM Used\",\n  ROUND(100.0 * r.clients_sm_total_amount / NULLIF(c.clients_sm, 0), 1) AS \"SM Util %\"\nFROM companies c\nJOIN reports r ON r.account_id = c.account_id AND r.report_type = 'clients_report'\nWHERE r.report_date = (\n  SELECT MAX(r2.report_date) FROM reports r2\n  WHERE r2.account_id = c.account_id AND r2.report_type = 'clients_report'\n)\n  AND {company_match_expr('c.name')}\n  AND {account_match_expr('c.account_id')}\n  AND c.clients_sm > 0\n  AND r.clients_sm_total_amount < c.clients_sm * 0.3\nORDER BY \"SM Util %\" ASC",
          "rawQueryText": "SELECT c.name AS \"Customer\", c.account_id AS \"Account ID\",\n  c.clients_sm AS \"SM Purchased\", r.clients_sm_total_amount AS \"SM Used\",\n  ROUND(100.0 * r.clients_sm_total_amount / NULLIF(c.clients_sm, 0), 1) AS \"SM Util %\"\nFROM companies c\nJOIN reports r ON r.account_id = c.account_id AND r.report_type = 'clients_report'\nWHERE r.report_date = (\n  SELECT MAX(r2.report_date) FROM reports r2\n  WHERE r2.account_id = c.account_id AND r2.report_type = 'clients_report'\n)\n  AND {company_match_expr('c.name')}\n  AND {account_match_expr('c.account_id')}\n  AND c.clients_sm > 0\n  AND r.clients_sm_total_amount < c.clients_sm * 0.3\nORDER BY \"SM Util %\" ASC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": []
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "cellOptions": {
              "type": "auto"
            }
          }
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    },
    {
      "type": "table",
      "title": "Stale Reporting (>35 days)",
      "gridPos": {
        "h": 8,
        "w": 24,
        "x": 0,
        "y": 124
      },
      "id": 38,
      "targets": [
        {
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": null
          },
          "queryText": "SELECT c.name AS \"Customer\", c.account_id AS \"Account ID\",\n  MAX(r.report_date) AS \"Last Report\",\n  CAST(julianday('now') - julianday(MAX(r.report_date)) AS INTEGER) AS \"Days Since Report\"\nFROM companies c\nLEFT JOIN reports r ON r.account_id = c.account_id AND r.report_type = 'clients_report'\nWHERE {company_match_expr('c.name')}\n  AND {account_match_expr('c.account_id')}\nGROUP BY c.account_id\nHAVING \"Days Since Report\" > 35 OR \"Last Report\" IS NULL\nORDER BY \"Days Since Report\" DESC",
          "rawQueryText": "SELECT c.name AS \"Customer\", c.account_id AS \"Account ID\",\n  MAX(r.report_date) AS \"Last Report\",\n  CAST(julianday('now') - julianday(MAX(r.report_date)) AS INTEGER) AS \"Days Since Report\"\nFROM companies c\nLEFT JOIN reports r ON r.account_id = c.account_id AND r.report_type = 'clients_report'\nWHERE {company_match_expr('c.name')}\n  AND {account_match_expr('c.account_id')}\nGROUP BY c.account_id\nHAVING \"Days Since Report\" > 35 OR \"Last Report\" IS NULL\nORDER BY \"Days Since Report\" DESC",
          "queryType": "table",
          "timeColumns": [],
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": []
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "cellOptions": {
              "type": "auto"
            }
          }
        },
        "overrides": []
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": null
      }
    }
  ],
  "refresh": "",
  "schemaVersion": 39,
  "style": "dark",
  "tags": [
    "akeyless",
    "customer-success",
    "usage",
    "technical",
    "portfolio"
  ],
  "templating": {
    "list": [
      {
        "name": "company_name",
        "type": "query",
        "label": "Company Name",
        "datasource": {
          "type": "frser-sqlite-datasource",
          "uid": null
        },
        "query": "SELECT DISTINCT name AS __value, name AS __text FROM companies WHERE name IS NOT NULL ORDER BY name",
        "refresh": 1,
        "includeAll": true,
        "multi": true,
        "allValue": "%"
      },
      {
        "name": "account_id",
        "type": "query",
        "label": "Account ID",
        "datasource": {
          "type": "frser-sqlite-datasource",
          "uid": null
        },
        "query": "SELECT DISTINCT account_id AS __value, name || ' (' || account_id || ')' AS __text FROM companies WHERE account_id IS NOT NULL AND ('${company_name:raw}' IN ('All', '%', '$__all') OR name IN (${company_name:sqlstring})) ORDER BY name, account_id",
        "refresh": 2,
        "includeAll": true,
        "multi": true,
        "allValue": "%"
      },
      {
        "name": "period_months",
        "type": "custom",
        "label": "Period",
        "query": "12 Months,3 Months",
        "options": [
          {
            "selected": true,
            "text": "12 Months",
            "value": "12 Months"
          },
          {
            "selected": false,
            "text": "3 Months",
            "value": "3 Months"
          }
        ],
        "current": {
          "selected": true,
          "text": "12 Months",
          "value": "12 Months"
        },
        "includeAll": false,
        "multi": false
      }
    ]
  },
  "time": {
    "from": "now-12M",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "browser",
  "title": "Akeyless CS \u2014 Portfolio Anomalies Dashboard",
  "uid": "akeyless-cs-portfolio-anomalies",
  "version": 25,
  "weekStart": ""
}